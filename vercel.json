{
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ],
  "buildCommand": "npm run build",
  "installCommand": "npm install",
  "framework": "vite",
  "env": {
    "ROLLUP_SKIP_LOAD_NATIVE_PLUGIN": "true"
  }
}
